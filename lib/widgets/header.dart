import 'package:flutter/material.dart';

AppBar header() {
  return AppBar(
    title: Text(
      "Home",
      style: TextStyle(fontSize: 20, fontFamily: "Signatra"),
    ),
    centerTitle: true,
  );
}
